# PROJECTS-AND-PAINS — standing context (fill once; agents read it first)

The one input every routing/scout/procurement block in this kit needs
(`[PROJECT_LIST_AND_PAINS]` / `{{PROJECT_LIST_AND_PAINS}}`). Fill it in and keep
it where your standing context lives (a private repo, your agent ledger, your
notes system) — every scout/router/procurement run reads it first. **Edit it,
don't worship it** — stale context misroutes every block that consumes it.

> Keep the FILLED copy private. It will name your projects, tools, accounts, and
> constraints; this public template ships blank on purpose.

## Who / what this is for

[OWNER ROLE — e.g. "owner-operator of a small manufacturing business; not a
software engineer by trade; builds serious systems with AI agents and wants
frontier-quality output without frontier-model babysitting or cost."]

## Projects (the garden)

| Project | What it is | State | The next fruit |
|---|---|---|---|
| [NAME] | [one line] | [tests/CI state, feature flags] | [the next concrete win] |
| [NAME] | [one line] | [LIVE — confirm-first? do-not-touch?] | [next step] |

## Standing pains (the weeds — what agents must actively fight)

1. **Frontier cost/limits** — [your quota/API reality]. → Router: frontier
   plans/reviews only; executors build; budget guard + spend reporting.
2. **Meta-decision overload** — which model, what effort, when to escalate. →
   The router block decides and DECLARES its route; the owner shouldn't have to.
3. **Token burn** — re-reading, re-deriving, re-explaining. → token-economy +
   token-budget-navigation; indexes and artifacts, not transcripts.
4. **Vibe-code defaults** — plausible-looking, unverified output. →
   PROOF-not-prose; gates; no stub shipped as done.
5. **Drift / forgetting** — rules fade over long sessions. → Locked doctrine in
   files + CI (never model memory); SEED→PROVE→LOCK→PRUNE lifecycle.
6. **Research bias / missed gems** — the same three famous answers; the great
   HF/arXiv/GitHub find never surfaces. → deep-procurement (reactive) +
   opportunity-scout (scheduled, mapped to THIS table, hype-vs-real scored).
7. **No proactivity** — agents answer, then go silent. → Scout cadence + advisor
   briefs that end in a smallest-experiment.
8. **Truncation / sycophancy / hallucination** — wants blunt, grounded, cited. →
   Retrieval-locked answers; citation validation; disconfirming-evidence quotas.
9. [YOUR domain-specific pain — e.g. "the deciding fact lives in the attachment,
   not the message body" → whole-artifact rule with text→vision fallback.]
10. [YOUR compliance/regulatory pain — every legitimate pathway, cited and
    risk-graded; a licensed professional validates the last mile.]

## Constraints (the fence)

- [Budget posture — e.g. prefer self-hosted / permissive-license /
  already-paid-for over new SaaS.]
- [Tenancy and identity home; where scheduled work runs; where builds run.]
- **The live system is sacred**: flags, interlocks, rollback, confirm-first
  ([OWNER]).
- **PII/secrets never in repos** — CI-enforced.
- [Your error asymmetry — which mistake is expensive here; tune recall on that
  side and let the cheap side absorb noise.]

## The team roster (subscriptions/tools agents may leverage — wire, don't buy)

[LIST what you already pay for: source control, error tracking, docs/notes, DB,
deploy targets, automation actuators, research sources, design tools, meeting
capture, the model provider(s). Most "AI can't do X" is an unwired subscription
you already have.]

## Operating defaults (how I want agents to behave, in one breath)

Route down, escalate on signal; prove before "done"; cite or say "not found";
brief me in plain English with industry terms in parentheses; end proposals with
the smallest experiment; log what was killed so it's never re-pitched; and never
let an existing rule veto exploration — gates veto *shipping*, nothing vetoes
*thinking*.
