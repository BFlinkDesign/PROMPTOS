# Evaluation Snapshots

Use this folder for intentional, reviewable evaluation result captures.

```powershell
npm run eval:snapshot
```

The default `npm run verify` gate does not write snapshots; it stays
deterministic and clean for CI.
