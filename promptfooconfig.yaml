description: PromptOS local no-API structural regression smoke; not behavioral evaluation

prompts:
  - file://tools/promptfoo-dynamic-prompt.js

providers:
  - id: echo
    label: local echo provider

tests:
  - description: scope pipeline preserves deterministic verification requirements
    vars:
      prompt_path: prompts/scope-pipeline.md
    assert:
      - type: contains
        value: Verify gate
      - type: contains
        value: trust tiers
      - type: contains
        value: recompute
  - file://tests/promptfoo-regression.json
